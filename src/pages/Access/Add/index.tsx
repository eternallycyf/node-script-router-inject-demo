import { Button } from "antd";
import { PageContainer } from "@ant-design/pro-layout";
const Add: React.FC = () => {
  return (
    <PageContainer>
      <Button>添加权限</Button>
    </PageContainer>
  );
};

export default Add;
